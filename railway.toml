[build]
builder = "nixpacks"
buildCommand = "chmod +x ./scripts/render_build.sh && ./scripts/render_build.sh"

[deploy]
startCommand = ".venv/bin/python -m uvicorn app.main:app --host 0.0.0.0 --port $PORT"
healthcheckPath = "/health"
restartPolicyType = "ON_FAILURE"
