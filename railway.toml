[build]
builder = "nixpacks"
buildCommand = "pip install -r requirements.txt && mkdir -p data storage/signatures storage/pdfs storage/id_documents storage/archive_exports && python scripts/run_all_migrations.py && python scripts/init_db.py"

[deploy]
startCommand = "uvicorn app.main:app --host 0.0.0.0 --port $PORT"
healthcheckPath = "/health"
restartPolicyType = "ON_FAILURE"
